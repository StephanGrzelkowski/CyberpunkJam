/// @description set up the ds grid based on room size
grid_rows = ceil(room_height / 32);
grid_columns = ceil(room_width / 32);

ds_electrical_grid = ds_grid_create(grid_columns, grid_rows)
 












